# チュートリアルで学ぶ HTML / CSS スキルのまとめ

MDN Blog 風ページを 6 ステップで作るチュートリアルで必要なスキルの一覧。

| ステップ | テーマ | 主なスキル |
| --- | --- | --- |
| Step 1 | HTML の基本コンテンツ | 文書構造・セマンティック要素・リンクと画像 |
| Step 2 | コンテンツ完成 + 基本 CSS | 外部リソース読み込み・SVG・CSS 変数・リセット |
| Step 3 | div によるグループ化 | `<div>` の使いどころ・コンテナパターン |
| Step 4 | 装飾の CSS | セレクタ・擬似クラス・ボックスの装飾 |
| Step 5 | レイアウトの CSS | Flexbox・Grid・画像の制御 |
| Step 6 | レスポンシブ | メディアクエリ・モバイルファースト |

---

## Step 1 — HTML の基本コンテンツ

### 文書の基本構造

| スキル | 説明 |
| --- | --- |
| `<!DOCTYPE html>` | HTML5 の文書であることの宣言。必ず 1 行目に書く |
| `<html lang="ja">` | ルート要素。`lang` はページの主要言語（読み上げ・翻訳に影響） |
| `<head>` / `<body>` | メタ情報（表示されない）と本文（表示される）の区別 |
| `<meta charset="UTF-8">` | 文字コードの指定。文字化け防止のため最初に書く |
| `<meta name="viewport">` | スマホでの表示幅の制御。レスポンシブ対応（Step 6）の前提 |
| `<title>` | ブラウザのタブに表示されるページ名 |
| `<!-- コメント -->` | ブラウザに表示されないメモ。セクションの区切りに使う |

### セマンティック要素（ページの骨組み）

| スキル | 説明 |
| --- | --- |
| `<header>` | ページ上部の導入部（ロゴ・ナビを入れる） |
| `<nav>` | ナビゲーション。`aria-label` で複数のナビを区別する |
| `<main>` | ページの主要コンテンツ。1 ページに 1 つ |
| `<article>` | それ単体で完結するコンテンツ（記事カード 1 件） |
| `<footer>` | ページ下部の情報 |
| `<h1>` / `<h2>` | 見出しの階層。`h1` はページに 1 つ、カードの見出しは `h2` |
| `<p>` | 段落 |
| `<span>` | 意味を持たないインラインの範囲指定（著者名・読了時間など） |

### リスト・リンク・画像

| スキル | 説明 |
| --- | --- |
| `<ul>` / `<li>` | 順序なしリスト（ナビメニュー） |
| `<a href="#">` | リンク。`#` は「リンク先が未定」のプレースホルダー |
| `<img src alt>` | 画像。意味のある画像は `alt` で説明、装飾的な画像は `alt=""` |
| `<time datetime="">` | 日付。`datetime` 属性に機械可読な形式（`2026-06-15`）を書く |
| `class` 属性 | CSS から要素を選ぶための「名前」。命名は役割ベース（`post-title` など） |
| `aria-label` 属性 | 支援技術向けのラベル（`<nav aria-label="メインメニュー">`） |

---

## Step 2 — HTML コンテンツの完成と基本の CSS

### HTML：外部リソースの読み込み

| スキル | 説明 |
| --- | --- |
| `<link rel="stylesheet">` | 外部 CSS ファイルの読み込み |
| Web フォント | Inter 公式配信（rsms.me）の CSS を読み込んで使う。配信元により含まれる OpenType 機能が異なる（Google Fonts 版 Inter は機能が削られている） |
| `<link rel="preconnect">` | フォント配信サーバーへ事前接続して読み込みを速くする定番パターン |
| リセット CSS（destyle.css） | 既存のリセットファイルを CDN から読み込む。デフォルトの余白・見出しサイズ・リストマーカーなどをまとめて消してくれる |

### HTML：追加のマークアップ

| スキル | 説明 |
| --- | --- |
| インライン SVG | `<svg>` `<path>` `viewBox` の役割。コードとして貼り付けられるベクター画像 |
| SVG のアクセシビリティ | `role="img"` + `<title>` で画像としての名前を与える |
| `<ol>` によるパンくず | 「トップ → 現在地」と順序に意味がある階層なので順序付きリストを使う |
| 構造の複製 | `<article>` ブロックをコピーして中身だけ書き換える（コンポーネント感覚） |

### CSS：デザイントークン

| スキル | 説明 |
| --- | --- |
| カスタムプロパティ | `:root { --text-primary: #1b1b1b; }` で色・フォントを一元管理 |
| `var()` | 定義した変数の参照。変更が 1 か所で済むメリット |
| カラーコード | `#1b1b1b` などの 16 進表記 |

### CSS：基本設定

| スキル | 説明 |
| --- | --- |
| セレクタの基本 | 要素セレクタ（`body` `img` `a`） |
| `box-sizing: border-box` | `width` に padding と border を含める。レイアウトの前提（destyle.css が全要素に設定済み） |
| `font-family` | フォント指定とフォールバック（Web フォント → OS 標準 → 総称ファミリー） |
| `font-feature-settings` | OpenType 機能の有効化。`"ss02"` で Inter の判別用字形（l に足が付くなど）を使う |
| `rem` 単位 | ルートの文字サイズ基準の相対単位。`px` との違い |
| `line-height` | 行間。単位なし数値で指定する理由 |
| `color` と継承 | `body` に指定した文字色が子要素へ継承される仕組み |
| `img { max-width: 100%; }` | 画像が親からはみ出さないようにする定番 |
| `img { display: block; }` | 画像下の余分なすき間（ベースライン由来）をなくす |
| `text-decoration` | リンクの下線の制御 |
| `list-style: none` | リストマーカーを消す（ナビやカード一覧をリストで作る前提） |

---

## Step 3 — div によるグループ化と共通コンテナ

| スキル | 説明 |
| --- | --- |
| `<div>` | 意味を持たないグループ化専用の要素。適切なセマンティック要素がないときだけ使う |
| セマンティック要素との使い分け | 「意味のまとまり」は `header` / `article` など、「見た目のまとまり」は `div` |
| `max-width` + `margin: 0 auto` | コンテンツ幅を制限してブロックを中央寄せする定番（コンテナパターン） |
| 共通クラスの再利用 | `.container` をヘッダー・サブ・ヘッダー・メイン・フッターで使い回す |
| スタイリングの下準備 | div とクラス名を先に整え、以降のステップを CSS の変更だけで進められるようにする |

---

## Step 4 — 応用の CSS（装飾）

### セレクタと擬似クラス

| スキル | 説明 |
| --- | --- |
| クラスセレクタ | `.post-title` のように class で選ぶ |
| 子孫セレクタ | `.nav-list a` で「ナビの中のリンクだけ」を選ぶ |
| `:hover` / `:active` | マウスオーバー時・クリック中のスタイル |
| 状態クラス | `.is-current` のように「状態」を class で表して見た目を変える |
| カスケードと詳細度 | `a` の共通スタイルを `.nav-list a` が上書きする仕組み（体感で学ぶ） |

### ボックスの装飾

| スキル | 説明 |
| --- | --- |
| `border` | 枠線のショートハンド（太さ・線種・色） |
| `border-radius` | 角丸。トークン化（`--card-radius`）と `50%` で正円（アバター） |
| `background-color` | 背景色。帯（サブ・ヘッダー、フッター）を色で表現する |
| `padding` の使い分け | 1 値・2 値・4 値のショートハンド |
| `overflow: hidden` | 角丸カードから内側の画像がはみ出さないようにする |
| `display: block` / `inline-block` | リンクに幅・高さ・padding を効かせるための display 変更 |

### テキストとボタン

| スキル | 説明 |
| --- | --- |
| `font-size` / `font-weight` | 文字の大きさと太さ。見出し・本文・補足のメリハリ |
| `text-align: center` | ページ見出しの中央揃え |
| リンクのボタン化 | `padding` + `background-color` + `border-radius` の定番テクニック |
| `transition` | 背景色の変化をなめらかにする（プロパティ名 + 時間） |

---

## Step 5 — レイアウトの CSS

### Flexbox（1 方向の配置）

| スキル | 説明 |
| --- | --- |
| `display: flex` | 子要素を横並びにする（ヘッダー、著者情報の行） |
| `flex-direction: column` | 縦積みの flex（カード内部） |
| `flex-wrap: wrap` | 入り切らないときに折り返す |
| `align-items: center` | 交差軸方向の中央揃え（ロゴとナビの高さを揃える） |
| `gap` | 子要素同士の間隔。2 値指定（行間と列間を別々に） |
| `flex-grow: 1` | 余った空間を埋めて伸びる（カード本文の高さを揃える） |
| `align-self` | 子要素単体の配置指定（Read more を右端へ） |
| `margin-top: auto` | flex 内で要素を末尾に押しやるテクニック（Read more を下端へ） |

### Grid（2 方向の整列）

| スキル | 説明 |
| --- | --- |
| `display: grid` | 格子状のレイアウト（記事一覧、フッター） |
| `grid-template-columns` | 列の定義。まずは `1fr` の 1 列から |
| `gap` | グリッドの隙間（Flexbox と共通の書き方） |
| Flexbox との使い分け | 1 方向の並びは flex、行×列の整列は grid |

### 画像とサイズの制御

| スキル | 説明 |
| --- | --- |
| `width: 100%` | サムネイルをカード幅いっぱいに広げる |
| `aspect-ratio` | 縦横比（`2 / 1`）を固定して高さを揃える |
| `object-fit: cover` | 比率の違う画像を切り抜いて枠にフィットさせる |

---

## Step 6 — レスポンシブ対応

| スキル | 説明 |
| --- | --- |
| `@media (min-width: …)` | 画面幅が一定以上のときだけ適用されるスタイル |
| モバイルファースト | ベースをモバイル（1 カラム）で書き、広い画面向けを上書きで足す |
| ブレイクポイント | 768px（タブレット）/ 1024px（デスクトップ）という代表的な境界 |
| 上書きの仕組み | 同じセレクタを後から書いて `grid-template-columns` だけ差し替える |
| `repeat()` と `fr` の比率 | `repeat(3, 1fr)` や `2fr 1fr 1fr 1fr` で列の本数・比率を指定 |
| 確認方法 | 開発者ツールのデバイスモードで境界の前後を確認する |

---

## このチュートリアルでは扱わないスキル（発展）

- フォームとボタン要素（`<form>` `<input>` `<button>`）
- CSS の `position`（`sticky` ヘッダーなど）・`z-index`
- アニメーション（`@keyframes`）・`transform`
- ダークモード対応（`prefers-color-scheme`）
- `<picture>` / `srcset` によるレスポンシブ画像

